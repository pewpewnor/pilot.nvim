name: Makefile CI

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs:
    lint:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: Check formatting
              uses: JohnnyMorganz/stylua-action@v5
              with:
                  token: ${{ secrets.GITHUB_TOKEN }}
                  version: 2.5.0
                  args: --check .

            - name: Run LuaLS diagnostics
              uses: mrcjkb/lua-typecheck-action@v1
              with:
                  checklevel: Warning

    build:
        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest, macos-latest, windows-latest]

        runs-on: ${{ matrix.os }}

        steps:
            - name: Checkout dove.nvim
              uses: actions/checkout@v4

            - name: Checkout plenary.nvim
              uses: actions/checkout@v4
              with:
                  repository: nvim-lua/plenary.nvim
                  path: .test-dependencies/plenary.nvim

            - name: Set up Neovim
              uses: rhysd/action-setup-vim@v1
              with:
                  neovim: true
                  version: v0.12.5

            - name: Install make on Windows
              if: runner.os == 'Windows'
              run: choco install make --no-progress --yes

            - name: Run tests
              run: make test
              env:
                  PLENARY_DIR: ${{ github.workspace }}/.test-dependencies/plenary.nvim
